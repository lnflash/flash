# This file contains secrets. 
# It should not be checked into git and it's permissions should be lifted to prevent an AI from reading it

ibex:
  email: 
  password: 
  webhook:
    uri: 

# Either create your own sandbox account, or get credentials to existing sandbox account
mailgun:
  apiKey: 
  domain: 

cashout:
  email:
    to: # if using mailgun, make sure this email has been added as a recipient address in the mailgun dashboard 