meta {
  name: globals
  type: graphql
  seq: 16
}

post {
  url: {{main_protocol}}://{{main_domain}}:{{main_port}}/graphql
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:graphql {
  query globals {
      globals {
          buildInformation {
              buildTime
              commitHash
              helmRevision
          }
          nodesIds
      }
  }
}

settings {
  encodeUrl: true
}
