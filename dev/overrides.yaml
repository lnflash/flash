# This file contains configuration overrides for the Flash backend

sendgrid:
  apiKey: "<your-sendgrid-api-key-here>"

# Cashout email configuration - where cashout notifications are sent
cashout:
  email:
    to: "support@getflash.io" # Change this to your admin email