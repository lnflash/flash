# future admins should be added via admin-api. 
admin_accounts:
  - role: "bankowner"
    phone: "+16505554334"

ibex:
  url: "https://api-sandbox.poweredbyibex.io"
  email: ""
  password: ""
  webhook:
    uri: "" # external uri defined in overrides
    port: 4008
    secret: "not-so-secret"

# See Foreign Exchange Rates: https://www.firstglobal-bank.com/
exchangeRates:
  USD:
    JMD:
      bid: 150
      ask: 160

cashout:
  enabled: false
  minimum:
    amount: 0
    currency: USD
  maximum:
    amount: 100 # $1
    currency: USD
  accountLevel: 2
  fee: 200 # basis points
  duration: 600 # seconds
  email:
    to: "" # add to the overrides.yaml
    from: "notifications@getflash.io"
    subject: "New Cashout"

mailgun:
  apiKey: "<replace>"
  domain: ""

topup:
  providers:
    fygaro:
      enabled: true
      webhook:
        secret: "fygaro-webhook-secret-placeholder" # TODO: Replace with actual Fygaro webhook secret
        path: "/webhooks/topup/fygaro"
      paymentButtonId: "bd4a34c1-3d24-4315-a2b8-627518f70916"
    stripe:
      enabled: false
      webhook:
        secret: ""
        path: "/webhooks/topup/stripe"
    paypal:
      enabled: false
      webhook:
        secret: ""
        path: "/webhooks/topup/paypal"
